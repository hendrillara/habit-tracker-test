Create a new IAS run (kebab-case slug required).

1) Run: `./scripts/ias new-run $ARGUMENTS`
2) Fill in the generated run artifacts under `docs/ias/runs/YYYYMMDD-$ARGUMENTS/`.
3) Follow `docs/ias/process/run-protocol.md`.

