Bootstrap IAS into a target repository path.

1) Run: `./scripts/ias bootstrap $ARGUMENTS`
2) In the target repo, update `docs/ias/project-context.md` and start a run with `./scripts/ias new-run <kebab-slug>`.

